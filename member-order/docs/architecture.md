# 會員服務訂購單系統 - 架構分析報告

> 由 Senior Architect Skill 自動產生
> 分析日期：2026-01-21

---

## 📋 專案概覽

| 項目         | 內容                                                |
| ------------ | --------------------------------------------------- |
| **專案名稱** | 吾飛藝術銀行 - 會員服務訂購單                       |
| **技術棧**   | React 18 + Tailwind CSS + Google Apps Script        |
| **部署架構** | Static HTML + Serverless Backend                    |
| **主要功能** | 訂購表單、Google Sheets 整合、LINE 推播、行事曆預約 |

---

## 🏗️ 系統架構圖

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           CLIENT (Browser)                               │
│  ┌─────────────┐   ┌─────────────────┐   ┌────────────────────────────┐ │
│  │  index.html │──▶│  order-form.html │──▶│   order-success.html       │ │
│  │  (Router)   │   │  (React SPA)     │   │   (React SPA)              │ │
│  └─────────────┘   └────────┬─────────┘   └────────────┬───────────────┘ │
│                             │                          │                 │
│  ┌──────────────────────────┴──────────────────────────┴───────────────┐ │
│  │                        config.js (Shared Config)                     │ │
│  │  • API Endpoints  • Products Data  • LINE Config  • Payment Info    │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                             │                                            │
│  ┌──────────────────────────┴───────────────────────────────────────────┐│
│  │                    styles.css (Shared Styles)                        ││
│  │  • Animations  • RWD  • Touch Optimization  • Safe Area              ││
│  └──────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTP/HTTPS
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    SERVERLESS BACKEND (Google Apps Script)               │
│  ┌─────────────────────────────┐   ┌─────────────────────────────────┐  │
│  │   doPost (Order API)        │   │   doGet (Push + Calendar API)   │  │
│  │   • Create Order            │   │   • pushOrderCard               │  │
│  │   • Update Order            │   │   • pushText                    │  │
│  │   • Write to Google Sheet   │   │   • getCalendarEvents           │  │
│  └──────────────┬──────────────┘   └──────────────┬──────────────────┘  │
│                 │                                  │                     │
│                 ▼                                  ▼                     │
│  ┌──────────────────────────┐   ┌──────────────────────────────────────┐│
│  │     Google Sheets        │   │   External APIs                      ││
│  │     (Database)           │   │   • LINE Messaging API               ││
│  │                          │   │   • Google Calendar API              ││
│  └──────────────────────────┘   └──────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 檔案結構分析

```
member-order/
├── 🌐 前端頁面 (Client)
│   ├── index.html           # 入口路由 - 自動導向填寫頁或成功頁
│   ├── order-form.html      # 主要訂購表單 (React SPA, ~1210 行)
│   ├── order-success.html   # 訂單成功頁 (React SPA, ~794 行)
│   ├── ui-simulation.html   # UI 模擬測試頁
│   ├── config.js            # 共用設定檔 (~165 行)
│   └── styles.css           # 共用樣式表 (~315 行)
│
├── 📜 後端腳本 (Serverless)
│   ├── get-post.txt              # Google Apps Script - 訂單 API
│   ├── api-proxy.txt             # Google Apps Script - LINE 推播 + Calendar
│   └── line-pay.txt              # Google Apps Script - LINE Pay 付款
│
├── 📖 文件
│   ├── requirement.md            # 原始需求規格
│   └── LINE-LOGIN-設定指南.md    # LINE Login 整合指南
```

---

## 🔄 資料流程圖

### 1️⃣ 訂單提交流程

```
┌──────────┐    填寫表單     ┌──────────────┐    POST     ┌─────────────────┐
│  客戶    │ ────────────▶  │ order-form   │ ─────────▶  │ Google Apps     │
│          │                │ .html        │             │ Script (doPost) │
└──────────┘                └──────────────┘             └────────┬────────┘
                                   │                              │
                                   │ localStorage                 │ appendRow
                                   ▼                              ▼
                            ┌──────────────┐             ┌─────────────────┐
                            │ 草稿暫存     │             │ Google Sheets   │
                            │ (DRAFT_KEY)  │             │ (訂單資料庫)    │
                            └──────────────┘             └─────────────────┘
                                   │
                                   │ 提交成功
                                   ▼
                            ┌──────────────┐
                            │ order-success│
                            │ .html        │
                            └──────────────┘
```

### 2️⃣ LINE 推播流程

```
┌──────────────┐   LIFF Login    ┌─────────────┐   JSONP      ┌─────────────────┐
│ order-success│ ─────────────▶  │ LINE        │ ──────────▶  │ Google Apps     │
│ .html        │                 │ Platform    │              │ Script (doGet)  │
└──────────────┘                 └─────────────┘              └────────┬────────┘
                                        │                              │
                                        │ userId                       │ POST
                                        ▼                              ▼
                                 ┌─────────────┐              ┌─────────────────┐
                                 │ 取得用戶資訊│              │ LINE Messaging  │
                                 └─────────────┘              │ API             │
                                                              └────────┬────────┘
                                                                       │
                                                                       │ Push Message
                                                                       ▼
                                                              ┌─────────────────┐
                                                              │ 客戶 LINE       │
                                                              │ 收到訂單卡片    │
                                                              └─────────────────┘
```

### 3️⃣ 行事曆整合流程

```
┌──────────────┐   GET request   ┌─────────────────┐   Calendar API   ┌─────────────────┐
│ order-form   │ ─────────────▶  │ Google Apps     │ ────────────────▶│ Google Calendar │
│ .html        │                 │ Script (doGet)  │                  │                 │
└──────────────┘                 └────────┬────────┘                  └────────┬────────┘
                                          │                                     │
                                          │                                     │ events
                                          ▼                                     ▼
                                 ┌─────────────────┐                   ┌─────────────────┐
                                 │ 顯示可預約      │ ◀─────────────────│ 返回事件列表    │
                                 │ 時段            │                   │                 │
                                 └─────────────────┘                   └─────────────────┘
```

---

## 🧩 元件架構分析

### order-form.html 元件結構

```
OrderFormPage (Root Component)
├── State Management
│   ├── consultantInfo        # 顧問資訊
│   ├── customerInfo          # 客戶資訊
│   ├── cart                  # 購物車
│   ├── signatureData         # 簽名資料
│   ├── calendarEvents        # 行事曆事件
│   └── errors                # 表單驗證錯誤
│
├── UI Sections
│   ├── TopBar                # 頂部導航
│   ├── ConsultantSection     # 顧問資料區塊
│   ├── CustomerSection       # 客戶資料區塊
│   ├── ProductSection        # 產品選擇區塊
│   │   └── ProductCard[]     # 產品卡片 (可展開)
│   ├── CalendarSection       # 預約行事曆
│   │   ├── CalendarGrid      # 日期格線
│   │   ├── DatePicker        # 日期選擇器
│   │   ├── PeriodDropdown    # 時段選擇
│   │   └── TimeDropdown      # 時間選擇
│   ├── SignatureSection      # 簽名區塊
│   └── FooterCTA             # 底部提交按鈕
│
└── Modals
    └── SignatureModal        # 簽名板彈窗
```

### order-success.html 元件結構

```
OrderSuccessPage (Root Component)
├── State Management
│   ├── orderResult           # 訂單結果
│   ├── lineProfile           # LINE 用戶資訊
│   ├── isSendingToLine       # 發送中狀態
│   └── lineSendResult        # 發送結果
│
├── UI Sections
│   ├── OrderSummary          # 訂單摘要
│   ├── PaymentInfo           # 付款資訊
│   │   └── CountdownTimer    # 倒數計時器
│   ├── LineSection           # LINE 整合區塊
│   └── ActionButtons         # 操作按鈕
│
└── Modals
    ├── LineQRModal           # LINE QR Code 彈窗
    ├── LineSentModal         # 發送結果彈窗
    └── ImagePreviewModal     # 圖片預覽彈窗
```

---

## 💾 資料模型

### 訂單資料結構 (localStorage: STORAGE_KEY)

```typescript
interface Order {
  id: string; // 訂單編號 (WFB-YYYYMMDD-XXXX)
  isUpdate: boolean; // 是否為更新
  consultant: {
    name: string; // 顧問姓名
    id: string; // 顧問編號
    location: string; // 服務據點
  };
  customer: {
    name: string; // 客戶姓名 *
    phone: string; // 聯絡電話 *
    birthday: string; // 生日 (YYYYMMDD)
    email: string; // 信箱 *
    address: string; // 地址
    company: string; // 公司名稱
    taxId: string; // 統一編號
    source: string; // 資訊來源 *
    notes: string; // 備註
    appointmentDate: string; // 預約日期 *
    appointmentPeriod: string; // 時段 *
    appointmentTime: string; // 時間 *
  };
  items: Record<number, number>; // 產品ID -> 數量
  total: number; // 總金額
  date: string; // 訂單日期
  signature: string; // 簽名 (Base64 DataURL)
  deadline: number; // 付款期限 (timestamp)

  // LINE 相關 (發送後填入)
  lineUserId?: string;
  lineDisplayName?: string;
  lineSentAt?: string;
}
```

### 產品資料結構 (config.js: APP_PRODUCTS)

```typescript
interface Product {
  id: number;
  name: string; // 產品名稱
  subName: string; // 英文副標題
  price: number; // 價格
  period: string; // 時長
  description: string; // 描述
  features: string[]; // 特點列表
  iconName: string; // Lucide 圖標名稱
  color: string; // Tailwind 漸層色
  popular?: boolean; // 是否熱門
}
```

---

## 🔒 安全性分析

### ✅ 已實作的安全措施

| 項目                      | 說明                                                                           |
| ------------------------- | ------------------------------------------------------------------------------ |
| **API Token 保護**        | LINE Channel Access Token 存放在 Google Apps Script 的腳本屬性中，不暴露於前端 |
| **Calendar API Key 保護** | Google Calendar API Key 同樣存於後端，透過代理存取                             |
| **CORS 繞過**             | 使用 JSONP 方式呼叫 Google Apps Script，避免 CORS 問題                         |
| **資料驗證**              | 前端表單驗證必填欄位                                                           |
| **簽名確認**              | 要求客戶簽名確認訂單內容                                                       |

### ⚠️ 潛在風險與建議

| 風險等級 | 項目                      | 問題描述                                 | 建議改善                           |
| -------- | ------------------------- | ---------------------------------------- | ---------------------------------- |
| 🔴 高    | **Google Sheet ID 暴露**  | `GOOGLE_SHEET_ID` 直接寫在前端 config.js | 移至後端，前端不需知道 Sheet ID    |
| 🔴 高    | **缺乏後端驗證**          | 訂單資料僅前端驗證，後端直接寫入         | 在 Apps Script 加入資料驗證        |
| 🟡 中    | **localStorage 敏感資料** | 訂單含簽名 Base64 存於 localStorage      | 考慮加密或縮短存活時間             |
| 🟡 中    | **無速率限制**            | Apps Script 無防止濫用機制               | 加入 IP/User 請求限制              |
| 🟢 低    | **LIFF ID 暴露**          | LINE Login Channel ID 在前端             | 這是正常做法，但可考慮使用環境變數 |

---

## 📊 效能分析

### 載入資源統計

| 資源         | 來源         | 大小估計    | 快取策略   |
| ------------ | ------------ | ----------- | ---------- |
| Tailwind CSS | CDN (unpkg)  | ~40KB gzip  | CDN 快取   |
| React 18     | ESM (esm.sh) | ~130KB gzip | CDN 快取   |
| Babel        | CDN (unpkg)  | ~200KB gzip | CDN 快取   |
| Lucide Icons | ESM (esm.sh) | ~5KB (按需) | CDN 快取   |
| html2canvas  | CDN          | ~100KB      | CDN 快取   |
| LIFF SDK     | LINE CDN     | ~50KB       | CDN 快取   |
| Google Fonts | Google       | ~20KB       | 瀏覽器快取 |

### 效能建議

1. **🔧 Bundle 優化**: 考慮使用 Vite/Webpack 預編譯 React，移除 Babel 執行時編譯
2. **🔧 Code Splitting**: 將 order-success.html 的 LINE 相關邏輯延遲載入
3. **🔧 圖片優化**: 簽名 Canvas 可壓縮後再存入 localStorage
4. **🔧 預載入**: 加入 `<link rel="preconnect">` 優化第三方資源載入

---

## 🔄 狀態管理分析

### 目前架構

```
┌─────────────────────────────────────────────────────────────┐
│                     React useState (Component Level)        │
├─────────────────────────────────────────────────────────────┤
│  order-form.html                order-success.html          │
│  ├── consultantInfo             ├── orderResult             │
│  ├── customerInfo               ├── lineProfile             │
│  ├── cart                       ├── isSendingToLine         │
│  ├── signatureData              └── lineSendResult          │
│  ├── calendarEvents                                         │
│  └── errors                                                 │
├─────────────────────────────────────────────────────────────┤
│                     localStorage (Persistent)               │
├─────────────────────────────────────────────────────────────┤
│  DRAFT_KEY (wufei_prod_v1_draft)                           │
│  └── 草稿自動儲存                                           │
│                                                             │
│  STORAGE_KEY (wufei_prod_v1_order)                         │
│  └── 已提交訂單 (有效期 24 小時)                            │
└─────────────────────────────────────────────────────────────┘
```

### 改善建議

對於目前規模的應用，useState + localStorage 已足夠。但若未來需要：

- 多頁面共享即時狀態 → 考慮 Zustand 或 React Context
- 伺服器狀態同步 → 考慮 React Query / SWR
- 複雜表單處理 → 考慮 React Hook Form

---

## 🚀 擴展性建議

### 短期優化 (Low Effort, High Impact)

| 優先序 | 項目              | 預估工時 | 效益              |
| ------ | ----------------- | -------- | ----------------- |
| 1      | 移除前端 Sheet ID | 2h       | 🔒 安全性大幅提升 |
| 2      | 後端資料驗證      | 4h       | 🔒 防止無效訂單   |
| 3      | 錯誤處理優化      | 3h       | 🎯 UX 改善        |
| 4      | Loading 狀態優化  | 2h       | 🎯 UX 改善        |

### 中期架構升級

```
目前架構:
HTML + CDN React ──▶ Google Apps Script ──▶ Google Sheets

建議升級 (若業務量增長):
                    ┌─────────────┐
                    │   Vercel    │
                    │   (CDN)     │
                    └──────┬──────┘
                           │
┌──────────────────────────┼──────────────────────────┐
│                    Next.js App                       │
├──────────────────────────┼──────────────────────────┤
│  /pages                  │  /api                    │
│  ├── order-form          │  ├── orders (REST)       │
│  └── order-success       │  ├── line-push           │
│                          │  └── calendar            │
└──────────────────────────┼──────────────────────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
       ┌──────────┐  ┌──────────┐  ┌──────────┐
       │ Supabase │  │ LINE API │  │ Google   │
       │ (DB)     │  │          │  │ Calendar │
       └──────────┘  └──────────┘  └──────────┘
```

### 升級效益

| 項目     | 目前             | 升級後            |
| -------- | ---------------- | ----------------- |
| 部署     | 手動複製到伺服器 | Git Push 自動部署 |
| 環境變數 | 寫死在 config.js | Vercel 環境變數   |
| API 保護 | 暴露於前端       | 後端安全代理      |
| 資料庫   | Google Sheets    | Supabase/Postgres |
| SEO      | 無               | Next.js SSR/SSG   |
| 型別安全 | 無               | TypeScript        |

---

## 📝 技術債清單

| ID     | 類型 | 描述                          | 影響 | 建議處理時間 |
| ------ | ---- | ----------------------------- | ---- | ------------ |
| TD-001 | 安全 | GOOGLE_SHEET_ID 暴露於前端    | 高   | 立即         |
| TD-002 | 安全 | 後端缺乏資料驗證              | 高   | 1 週內       |
| TD-003 | 效能 | 使用 Babel 執行時編譯         | 中   | 1 個月內     |
| TD-004 | 維護 | 單一 HTML 檔案過大 (1200+ 行) | 中   | 有空時       |
| TD-005 | 測試 | 無自動化測試                  | 中   | 2 個月內     |
| TD-006 | 文件 | API 文件不完整                | 低   | 有空時       |

---

## ✅ 整體評估

### 優點

- ✅ **快速開發**: 直接使用 CDN + Google Apps Script，零建置成本
- ✅ **維護簡單**: 單一 HTML 檔案，易於理解和修改
- ✅ **成本低廉**: 使用 Google 免費服務，無伺服器費用
- ✅ **RWD 設計**: 支援手機、平板、桌面多裝置
- ✅ **UX 細節**: 草稿自動儲存、簽名板、動畫效果

### 改善空間

- ⚠️ 安全性需加強（API 端點、資料驗證）
- ⚠️ 可擴展性受限於 Google Sheets 效能
- ⚠️ 缺乏自動化測試
- ⚠️ 程式碼未模組化，維護成本將隨功能增加而上升

### 適用場景

此架構適合：

- 📊 月訂單量 < 1,000 筆
- 👥 同時在線用戶 < 50 人
- 🔄 功能變動頻率低
- 💰 預算有限的小型專案

---

## 📚 相關文件

- [requirement.md](./requirement.md) - 原始需求規格
- [line-login-manual.md](./line-login-manual.md) - LINE Login 整合指南
- [line-pay-manual.md](./line-pay-manual.md) - LINE Pay 整合指南
- [get-post.txt](../google-apps-script/get-post.txt) - 訂單 API 程式碼
- [api-proxy.txt](../google-apps-script/api-proxy.txt) - 推播 + Calendar API 程式碼
- [line-pay.txt](../google-apps-script/line-pay.txt) - LINE Pay API 程式碼

---

_此文件由 Senior Architect Skill 自動產生，建議定期更新以反映系統變更。_
